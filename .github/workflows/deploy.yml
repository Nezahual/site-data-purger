name: Deploy Extension in Chrome Web Store

on:
  release:
    types: [published] 

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Verify secrets  
        run: |
          echo "Checking secrets..."  
          echo "EXTENSION_ID: ${{ secrets.EXTENSION_ID != '' && 'SET' || 'MISSING' }}"  
          echo "CHROME_CLIENT_ID: ${{ secrets.CHROME_CLIENT_ID != '' && 'SET' || 'MISSING' }}"  
          echo "CHROME_CLIENT_SECRET: ${{ secrets.CHROME_CLIENT_SECRET != '' && 'SET' || 'MISSING' }}"  
          echo "CHROME_REFRESH_TOKEN: ${{ secrets.CHROME_REFRESH_TOKEN != '' && 'SET' || 'MISSING' }}"

      - name: Zip extension
        run: |
          zip -r SiteDataPurger.zip . -x "*.git/*" "*.github/*" "README.md"

      - name: Upload to Chrome Web Store
        uses: mnao305/chrome-extension-upload@v6.0.0
        with:
          file-path: ./SiteDataPurger.zip
          extension-id: ${{ secrets.EXTENSION_ID }}
          client-id: ${{ secrets.CHROME_CLIENT_ID }}
          client-secret: ${{ secrets.CHROME_CLIENT_SECRET }}
          refresh-token: ${{ secrets.CHROME_REFRESH_TOKEN }}
          publish: false